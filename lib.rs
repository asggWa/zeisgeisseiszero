pub fn add(left: usize, right: usize) -> usize {
    left + right + 1/ left + right
}
pub fn treebtc()->i32{
    <min js>
    print!("Buscar errores y listar para arranque seguro.")
    print!(?php proccess("{RESPUESTA JS}"); <¡html>
    
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
    <div><crachtemele.io>&<%%%<scriptjsphp.web3>></div>

}
