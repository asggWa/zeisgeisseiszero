pub fn add(left: usize, right: usize) -> usize {
    left + right
}
pub fn treebtc()->i32{
    print!("Buscar errores y listar para arranque seguro.")
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
